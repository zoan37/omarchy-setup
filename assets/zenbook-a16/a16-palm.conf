# /etc/default/a16-palm: read by a16-palm-filter.service. Values only (systemd keeps inline comments as part
# of the value). Defaults are in the script header; uncomment to override, then
# `sudo systemctl restart a16-palm-filter`.
# TYPING_GAP=0.5
# RECENT=1.2
# MOVE_MM=4
# EDGE_MM=10
# TOP_MM=8
# DEBUG=1
