# Let asusd own CPU EPP (profile_quiet_epp = BalancePerformance in /etc/asusd/asusd.ron).
# PPD power-saver otherwise forces EPP=power and caps the i9-12900H at ~1.7 GHz.
[Service]
ExecStart=
ExecStart=/usr/lib/power-profiles-daemon --block-driver=intel_pstate
